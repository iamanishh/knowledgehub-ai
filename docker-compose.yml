services:

  api:

    build: .

    container_name: knowledgehub-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads

    restart: unless-stopped