services:

  api:

    build: .

    container_name: knowledgehub-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./chroma_db:/app/chroma_db

    restart: unless-stopped